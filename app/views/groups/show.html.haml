- if cookies.signed[:user_id]
  %h3 You're the group owner.

%p Share this URL. Anyone joining will automatically be synced up.

%input(type="hidden" id="user_id" value="#{cookies.signed[:user_id]}")
%iframe(width="100%" height="465" scrolling="no" frameborder="no" src="https://w.soundcloud.com/player/?url=#{group.url}")

%p(style="font-size: 1.1vh; color: #aaa;") Latency is not being accounted for yet. Sorry!
